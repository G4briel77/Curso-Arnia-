const io = require('../curso Arnia/io/io')

io.write("este programa irá escrever os numeros de 0 a 100 em ordem descrente")

io.write("Preparado?")

let contador = 100

for (let contador = 100;contador >=1; contador--) {
    io.write(contador)
    
}
