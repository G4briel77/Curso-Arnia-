const io = require('../curso Arnia/io/io')

const array = [1,5,100,400,600,7,3,5,9,999,455]

const processarArray = (numeros) => {
    numeros.numeros.filter(numero) = numero % 2 !== 0
}
console.log(numeros)
